import React, {FC} from 'react';
import FormLogin from "../components/form.login/FormLogin";

const FormLoginPage:FC = () => {
    return (
        <div>
             <FormLogin/>
        </div>
    );
};

export default FormLoginPage;